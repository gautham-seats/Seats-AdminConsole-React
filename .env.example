# Copy to .env.local for development. Never commit real values.

# Compiled into the client bundle at build time. 'false' = read-only console
# (every non-GET request is refused in the browser). A production build fails
# unless this is set explicitly to 'true' or 'false'.
ADMIN_ALLOW_WRITES=false
