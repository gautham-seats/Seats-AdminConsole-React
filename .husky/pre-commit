npx lint-staged --max-arg-length 4000
npm run typecheck
# Dead exports and dead tokens failed CI on 2026-09-23 after passing every local check; catch them here.
# The bundle budget stays in pre-push: it measures a build, and pre-commit has none.
npm run lint:dead
npm run lint:tokens
staged=$(git diff --cached --name-only --diff-filter=ACMR -- "*.ts" "*.tsx")
if [ -n "$staged" ]; then
  npx jest --passWithNoTests --findRelatedTests $staged
fi
